Fix configuration cutout removing all stackup layers when the ``pyedb_cutout`` document layer is appended. ``Stackup.add_document_layer`` now creates a proper non-stackup layer instead of inserting a signal stackup layer and mutating its type.

